machine:
  services:
    - docker

dependencies:
  pre:
    - npm install -g clusternator

deployment:
  hub:
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_UESR -p $DOCKER_PASSWORD
      - docker build -t rafkhan/hello-world .
      - docker push rafkhan/hello-world
